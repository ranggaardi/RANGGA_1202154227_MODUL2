<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.rangga_1202154227_modul2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AwalActivity"
            android:label="@string/app_name" />
        <activity android:name=".TakeAwayActivity"  android:label="RANGGA_1202154227_MODUL2"
            android:parentActivityName=".AwalActivity"/>
        <activity android:name=".DineInActivity"
            android:label="@string/app_name"
            android:parentActivityName=".AwalActivity"
            />
        <activity android:name=".DaftarMenuActivity"
            android:label="@string/app_name"
            android:parentActivityName=".AwalActivity"/>

        <activity android:name=".DetailMenuActivity"
            android:label="@string/app_name"
            android:parentActivityName=".DaftarMenuActivity">

        </activity>
    </application>

</manifest>